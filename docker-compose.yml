version: '3.7'

services:
    microservice:
        build:
            context: ./news_aggregator
        image: news-maker
        container_name: news-maker
        restart: unless-stopped

        command: ["python3", "main.py"]